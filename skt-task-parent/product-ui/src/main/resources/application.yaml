server:
  port: 8081

spring:
  mvc:
    view:
      prefix: /WEB-INF/jsp/
      suffix: .jsp
  rabbitmq:
    addresses: 'localhost:5677'
    username: ${RABBITMQ_USERNAME}
    password: ${RABBITMQ_PASS}